nelmio_api_doc:
    documentation:
        info:
            title: Statsaver
            description: API for storing events from JIVO
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
